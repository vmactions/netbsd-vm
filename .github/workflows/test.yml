
name: Test

on:
  workflow_dispatch:
  release:
    types: [ published ]
  push:
    branches:
      - '*'
    paths:
      - '**.sh'
      - '.github/workflows/test.yml'
      - 'index.js'
      - 'conf/*'
      - 'hooks/*'



concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true



jobs:
  testsshfs:
    strategy:
      fail-fast: false
      matrix:
        release: [ "9.0", "9.1", "9.2", "9.3", "9.4", "10.0", "10.1"
, ""]
        arch: [ "aarch64", "", "riscv64", "powerpc64", "sparc64", "ppc64le", "s390x"]
        runs: [ "ubuntu-24.04", "ubuntu-24.04-arm"]
        exclude:
          - runs: "ubuntu-24.04-arm"
            arch: ""
          - runs: "ubuntu-24.04-arm"
            arch: "riscv64"
          - runs: "ubuntu-24.04-arm"
            arch: "powerpc64"
          - runs: "ubuntu-24.04-arm"
            arch: "sparc64"
          - runs: "ubuntu-24.04-arm"
            arch: "ppc64le"
          - runs: "ubuntu-24.04-arm"
            arch: "s390x"
          # Manual matrix excludes only; combos with no conf are auto-excluded.
          - release: "9.0"
            arch: aarch64
          - release: "9.0"
            arch: riscv64
          - release: "9.0"
            arch: powerpc64
          - release: "9.0"
            arch: sparc64
          - release: "9.0"
            arch: ppc64le
          - release: "9.0"
            arch: s390x
          - release: "9.1"
            arch: aarch64
          - release: "9.1"
            arch: riscv64
          - release: "9.1"
            arch: powerpc64
          - release: "9.1"
            arch: sparc64
          - release: "9.1"
            arch: ppc64le
          - release: "9.1"
            arch: s390x
          - release: "9.2"
            arch: aarch64
          - release: "9.2"
            arch: riscv64
          - release: "9.2"
            arch: powerpc64
          - release: "9.2"
            arch: sparc64
          - release: "9.2"
            arch: ppc64le
          - release: "9.2"
            arch: s390x
          - release: "9.3"
            arch: aarch64
          - release: "9.3"
            arch: riscv64
          - release: "9.3"
            arch: powerpc64
          - release: "9.3"
            arch: sparc64
          - release: "9.3"
            arch: ppc64le
          - release: "9.3"
            arch: s390x
          - release: "9.4"
            arch: aarch64
          - release: "9.4"
            arch: riscv64
          - release: "9.4"
            arch: powerpc64
          - release: "9.4"
            arch: sparc64
          - release: "9.4"
            arch: ppc64le
          - release: "9.4"
            arch: s390x
          - release: "10.0"
            arch: riscv64
          - release: "10.0"
            arch: powerpc64
          - release: "10.0"
            arch: ppc64le
          - release: "10.0"
            arch: s390x
          - release: "10.1"
            arch: riscv64
          - release: "10.1"
            arch: powerpc64
          - release: "10.1"
            arch: ppc64le
          - release: "10.1"
            arch: s390x
          - release: ""
            arch: riscv64
          - release: ""
            arch: powerpc64
          - release: ""
            arch: ppc64le
          - release: ""
            arch: s390x


    runs-on: ${{ matrix.runs }}
    name: sshfs NetBSD
    env:
      MYTOKEN : ${{ secrets.MYTOKEN }}
      MYTOKEN2: "value2"
      DEBUG: 1
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
    steps:
    - uses: actions/checkout@v6
#    - name: Creating Web console
#      if: ${{ contains(fromJSON('{"x86_64":["rsync","scp","sshfs","nfs"],"aarch64":["rsync","scp","sshfs","nfs"],"riscv64":[],"powerpc64":[],"sparc64":["rsync","scp","sshfs","nfs"],"ppc64le":[],"s390x":[]}')[matrix.arch == '' && 'x86_64' || matrix.arch], 'sshfs') }}
#      uses: vmactions/cf-tunnel@v0
#      id: tunnel
#      with:
#        protocol: http
#        port: 8000

    - name: Test in NetBSD
      if: ${{ contains(fromJSON('{"x86_64":["rsync","scp","sshfs","nfs"],"aarch64":["rsync","scp","sshfs","nfs"],"riscv64":[],"powerpc64":[],"sparc64":["rsync","scp","sshfs","nfs"],"ppc64le":[],"s390x":[]}')[matrix.arch == '' && 'x86_64' || matrix.arch], 'sshfs') }}
      id: test
      uses: vmactions/netbsd-vm@d06a31b989792ff5af44a2913cd49457cbe86c71
      with:
        envs: 'DEBUG MYTOKEN MYTOKEN2'
        prepare: |
          /usr/sbin/pkg_add -u curl
        mem: 4096
        release: ${{ matrix.release }}
        arch: ${{ matrix.arch }}
        nat: |
          "20022": "22"
          "8080": "80"
          "8443": "443"
          udp:"8081": "80"
        usesh: true
        debug: true
        debug-on-error: ${{ vars.DEBUG_ON_ERROR }}
        sync: sshfs
        run: |
          set -e
          ls -lah $HOME/work
          if [ -z "$(ls -A $HOME/work)" ]; then
            echo "sshfs error."
            exit 1
          fi
          echo "TEST_ENV=abc" >>${GITHUB_ENV}
          echo "textfromvm" >$GITHUB_WORKSPACE/vm.txt
          cd $GITHUB_WORKSPACE;
          ls -lah
          whoami
          env
          /sbin/sysctl hw.model
          /sbin/sysctl hw.ncpu
          /sbin/sysctl hw.physmem
          /sbin/sysctl hw.usermem
          /usr/bin/vmstat


    - name: Test NAT
      if: ${{ contains(fromJSON('{"x86_64":["rsync","scp","sshfs","nfs"],"aarch64":["rsync","scp","sshfs","nfs"],"riscv64":[],"powerpc64":[],"sparc64":["rsync","scp","sshfs","nfs"],"ppc64le":[],"s390x":[]}')[matrix.arch == '' && 'x86_64' || matrix.arch], 'sshfs') }}
      env:
        VM_USER: ""
      run: |
        set -e
        echo "TEST_ENV=$TEST_ENV"
        [ "$TEST_ENV" = "abc" ]
        ssh -v -p 20022 ${VM_USER:-root}@localhost  "uname -a;whoami;pwd"
    - name: Test custom shell
      if: ${{ contains(fromJSON('{"x86_64":["rsync","scp","sshfs","nfs"],"aarch64":["rsync","scp","sshfs","nfs"],"riscv64":[],"powerpc64":[],"sparc64":["rsync","scp","sshfs","nfs"],"ppc64le":[],"s390x":[]}')[matrix.arch == '' && 'x86_64' || matrix.arch], 'sshfs') }}
      shell: netbsd {0}
      run: |
        pwd
          cd $GITHUB_WORKSPACE;
          ls -lah
          whoami
          env
          /sbin/sysctl hw.model
          /sbin/sysctl hw.ncpu
          /sbin/sysctl hw.physmem
          /sbin/sysctl hw.usermem
          /usr/bin/vmstat







  testnfs:
    strategy:
      fail-fast: false
      matrix:
        release: [ "9.0", "9.1", "9.2", "9.3", "9.4", "10.0", "10.1"
, ""]
        arch: [ "aarch64", "", "riscv64", "powerpc64", "sparc64", "ppc64le", "s390x"]
        runs: [ "ubuntu-24.04", "ubuntu-24.04-arm"]
        exclude:
          - runs: "ubuntu-24.04-arm"
            arch: ""
          - runs: "ubuntu-24.04-arm"
            arch: "riscv64"
          - runs: "ubuntu-24.04-arm"
            arch: "powerpc64"
          - runs: "ubuntu-24.04-arm"
            arch: "sparc64"
          - runs: "ubuntu-24.04-arm"
            arch: "ppc64le"
          - runs: "ubuntu-24.04-arm"
            arch: "s390x"
          # Manual matrix excludes only; combos with no conf are auto-excluded.
          - release: "9.0"
            arch: aarch64
          - release: "9.0"
            arch: riscv64
          - release: "9.0"
            arch: powerpc64
          - release: "9.0"
            arch: sparc64
          - release: "9.0"
            arch: ppc64le
          - release: "9.0"
            arch: s390x
          - release: "9.1"
            arch: aarch64
          - release: "9.1"
            arch: riscv64
          - release: "9.1"
            arch: powerpc64
          - release: "9.1"
            arch: sparc64
          - release: "9.1"
            arch: ppc64le
          - release: "9.1"
            arch: s390x
          - release: "9.2"
            arch: aarch64
          - release: "9.2"
            arch: riscv64
          - release: "9.2"
            arch: powerpc64
          - release: "9.2"
            arch: sparc64
          - release: "9.2"
            arch: ppc64le
          - release: "9.2"
            arch: s390x
          - release: "9.3"
            arch: aarch64
          - release: "9.3"
            arch: riscv64
          - release: "9.3"
            arch: powerpc64
          - release: "9.3"
            arch: sparc64
          - release: "9.3"
            arch: ppc64le
          - release: "9.3"
            arch: s390x
          - release: "9.4"
            arch: aarch64
          - release: "9.4"
            arch: riscv64
          - release: "9.4"
            arch: powerpc64
          - release: "9.4"
            arch: sparc64
          - release: "9.4"
            arch: ppc64le
          - release: "9.4"
            arch: s390x
          - release: "10.0"
            arch: riscv64
          - release: "10.0"
            arch: powerpc64
          - release: "10.0"
            arch: ppc64le
          - release: "10.0"
            arch: s390x
          - release: "10.1"
            arch: riscv64
          - release: "10.1"
            arch: powerpc64
          - release: "10.1"
            arch: ppc64le
          - release: "10.1"
            arch: s390x
          - release: ""
            arch: riscv64
          - release: ""
            arch: powerpc64
          - release: ""
            arch: ppc64le
          - release: ""
            arch: s390x


    runs-on: ${{ matrix.runs }}
    name: nfs NetBSD
    env:
      MYTOKEN : ${{ secrets.MYTOKEN }}
      MYTOKEN2: "value2"
      DEBUG: 1
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
    steps:
    - uses: actions/checkout@v6
    - name: Test in NetBSD
      if: ${{ contains(fromJSON('{"x86_64":["rsync","scp","sshfs","nfs"],"aarch64":["rsync","scp","sshfs","nfs"],"riscv64":[],"powerpc64":[],"sparc64":["rsync","scp","sshfs","nfs"],"ppc64le":[],"s390x":[]}')[matrix.arch == '' && 'x86_64' || matrix.arch], 'nfs') }}
      id: test
      uses: vmactions/netbsd-vm@d06a31b989792ff5af44a2913cd49457cbe86c71
      with:
        envs: 'DEBUG MYTOKEN MYTOKEN2'
        prepare: |
          /usr/sbin/pkg_add -u curl
        mem: 4096
        release: ${{ matrix.release }}
        arch: ${{ matrix.arch }}
        nat: |
          "20022": "22"
          "8080": "80"
          "8443": "443"
          udp:"8081": "80"
        usesh: true
        debug: true
        debug-on-error: ${{ vars.DEBUG_ON_ERROR }}
        sync: nfs
        run: |
          ls -lah $HOME/work
          if [ -z "$(ls -A $HOME/work)" ]; then
            echo "nfs error."
            exit 1
          fi
          env
          echo "TEST_ENV=abc" >>${GITHUB_ENV}
          echo "textfromvm" >$GITHUB_WORKSPACE/vm.txt
          cd $GITHUB_WORKSPACE;
          ls -lah
          whoami
          env
          /sbin/sysctl hw.model
          /sbin/sysctl hw.ncpu
          /sbin/sysctl hw.physmem
          /sbin/sysctl hw.usermem
          /usr/bin/vmstat


    - name: Test NAT
      if: ${{ contains(fromJSON('{"x86_64":["rsync","scp","sshfs","nfs"],"aarch64":["rsync","scp","sshfs","nfs"],"riscv64":[],"powerpc64":[],"sparc64":["rsync","scp","sshfs","nfs"],"ppc64le":[],"s390x":[]}')[matrix.arch == '' && 'x86_64' || matrix.arch], 'nfs') }}
      env:
        VM_USER: ""
      run: |
        set -e
        [ -e "$GITHUB_WORKSPACE/vm.txt" ];
        cat "$GITHUB_WORKSPACE/vm.txt";
        grep "textfromvm" $GITHUB_WORKSPACE/vm.txt;
        echo "TEST_ENV=$TEST_ENV"
        [ "$TEST_ENV" = "abc" ]
        ssh -v -p 20022 ${VM_USER:-root}@localhost "uname -a;whoami;pwd"
    - name: Test custom shell
      if: ${{ contains(fromJSON('{"x86_64":["rsync","scp","sshfs","nfs"],"aarch64":["rsync","scp","sshfs","nfs"],"riscv64":[],"powerpc64":[],"sparc64":["rsync","scp","sshfs","nfs"],"ppc64le":[],"s390x":[]}')[matrix.arch == '' && 'x86_64' || matrix.arch], 'nfs') }}
      shell: netbsd {0}
      run: |
        pwd
          cd $GITHUB_WORKSPACE;
          ls -lah
          whoami
          env
          /sbin/sysctl hw.model
          /sbin/sysctl hw.ncpu
          /sbin/sysctl hw.physmem
          /sbin/sysctl hw.usermem
          /usr/bin/vmstat





  testscp:
    strategy:
      fail-fast: false
      matrix:
        release: [ "9.0", "9.1", "9.2", "9.3", "9.4", "10.0", "10.1"
, ""]
        arch: [ "aarch64", "", "riscv64", "powerpc64", "sparc64", "ppc64le", "s390x"]
        runs: [ "ubuntu-24.04", "ubuntu-24.04-arm"]
        exclude:
          - runs: "ubuntu-24.04-arm"
            arch: ""
          - runs: "ubuntu-24.04-arm"
            arch: "riscv64"
          - runs: "ubuntu-24.04-arm"
            arch: "powerpc64"
          - runs: "ubuntu-24.04-arm"
            arch: "sparc64"
          - runs: "ubuntu-24.04-arm"
            arch: "ppc64le"
          - runs: "ubuntu-24.04-arm"
            arch: "s390x"
          # Manual matrix excludes only; combos with no conf are auto-excluded.
          - release: "9.0"
            arch: aarch64
          - release: "9.0"
            arch: riscv64
          - release: "9.0"
            arch: powerpc64
          - release: "9.0"
            arch: sparc64
          - release: "9.0"
            arch: ppc64le
          - release: "9.0"
            arch: s390x
          - release: "9.1"
            arch: aarch64
          - release: "9.1"
            arch: riscv64
          - release: "9.1"
            arch: powerpc64
          - release: "9.1"
            arch: sparc64
          - release: "9.1"
            arch: ppc64le
          - release: "9.1"
            arch: s390x
          - release: "9.2"
            arch: aarch64
          - release: "9.2"
            arch: riscv64
          - release: "9.2"
            arch: powerpc64
          - release: "9.2"
            arch: sparc64
          - release: "9.2"
            arch: ppc64le
          - release: "9.2"
            arch: s390x
          - release: "9.3"
            arch: aarch64
          - release: "9.3"
            arch: riscv64
          - release: "9.3"
            arch: powerpc64
          - release: "9.3"
            arch: sparc64
          - release: "9.3"
            arch: ppc64le
          - release: "9.3"
            arch: s390x
          - release: "9.4"
            arch: aarch64
          - release: "9.4"
            arch: riscv64
          - release: "9.4"
            arch: powerpc64
          - release: "9.4"
            arch: sparc64
          - release: "9.4"
            arch: ppc64le
          - release: "9.4"
            arch: s390x
          - release: "10.0"
            arch: riscv64
          - release: "10.0"
            arch: powerpc64
          - release: "10.0"
            arch: ppc64le
          - release: "10.0"
            arch: s390x
          - release: "10.1"
            arch: riscv64
          - release: "10.1"
            arch: powerpc64
          - release: "10.1"
            arch: ppc64le
          - release: "10.1"
            arch: s390x
          - release: ""
            arch: riscv64
          - release: ""
            arch: powerpc64
          - release: ""
            arch: ppc64le
          - release: ""
            arch: s390x


    runs-on: ${{ matrix.runs }}
    name: scp NetBSD
    env:
      MYTOKEN : ${{ secrets.MYTOKEN }}
      MYTOKEN2: "value2"
      DEBUG: 1
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
    steps:
    - uses: actions/checkout@v6
#    - name: Creating Web console
#      if: ${{ contains(fromJSON('{"x86_64":["rsync","scp","sshfs","nfs"],"aarch64":["rsync","scp","sshfs","nfs"],"riscv64":[],"powerpc64":[],"sparc64":["rsync","scp","sshfs","nfs"],"ppc64le":[],"s390x":[]}')[matrix.arch == '' && 'x86_64' || matrix.arch], 'scp') }}
#      uses: vmactions/cf-tunnel@v0
#      id: tunnel
#      with:
#        protocol: http
#        port: 8000
    - name: Test in NetBSD
      if: ${{ contains(fromJSON('{"x86_64":["rsync","scp","sshfs","nfs"],"aarch64":["rsync","scp","sshfs","nfs"],"riscv64":[],"powerpc64":[],"sparc64":["rsync","scp","sshfs","nfs"],"ppc64le":[],"s390x":[]}')[matrix.arch == '' && 'x86_64' || matrix.arch], 'scp') }}
      id: test
      uses: vmactions/netbsd-vm@d06a31b989792ff5af44a2913cd49457cbe86c71
      with:
        envs: 'DEBUG MYTOKEN MYTOKEN2'
        prepare: |
          /usr/sbin/pkg_add -u curl
        mem: 4096
        release: ${{ matrix.release }}
        arch: ${{ matrix.arch }}
        nat: |
          "20022": "22"
          "8080": "80"
          "8443": "443"
          udp:"8081": "80"
        usesh: true
        debug: true
        debug-on-error: ${{ vars.DEBUG_ON_ERROR }}
        sync: scp
        run: |
          ls -lah $HOME/work
          #don't use tree here, freebsd riscv64 doesn't have a tree
          #tree $HOME/work/
          if [ -z "$(ls -A $HOME/work)" ]; then
            echo "nfs error."
            exit 1
          fi
          env
          echo "TEST_ENV=abc" >>${GITHUB_ENV}
          echo "textfromvm" >$GITHUB_WORKSPACE/vm.txt
          cd $GITHUB_WORKSPACE;
          ls -lah
          whoami
          env
          /sbin/sysctl hw.model
          /sbin/sysctl hw.ncpu
          /sbin/sysctl hw.physmem
          /sbin/sysctl hw.usermem
          /usr/bin/vmstat


    - name: Test files
      if: ${{ contains(fromJSON('{"x86_64":["rsync","scp","sshfs","nfs"],"aarch64":["rsync","scp","sshfs","nfs"],"riscv64":[],"powerpc64":[],"sparc64":["rsync","scp","sshfs","nfs"],"ppc64le":[],"s390x":[]}')[matrix.arch == '' && 'x86_64' || matrix.arch], 'scp') }}
      env:
        VM_USER: ""
      run: |
        set -e
        [ -e "$GITHUB_WORKSPACE/vm.txt" ];
        cat "$GITHUB_WORKSPACE/vm.txt";
        grep "textfromvm" $GITHUB_WORKSPACE/vm.txt;
        cat ${GITHUB_ENV}
        echo "TEST_ENV=$TEST_ENV";
        [ "$TEST_ENV" = "abc" ];





  test:
    strategy:
      fail-fast: false
      matrix:
        release: [ "9.0", "9.1", "9.2", "9.3", "9.4", "10.0", "10.1"
, ""]
        arch: [ "aarch64", "", "riscv64", "powerpc64", "sparc64", "ppc64le", "s390x"]
        runs: [ "ubuntu-24.04", "ubuntu-24.04-arm"]
        disableCache: [ "", "true"]
        exclude:
          - runs: "ubuntu-24.04-arm"
            arch: ""
          - runs: "ubuntu-24.04-arm"
            arch: "riscv64"
          - runs: "ubuntu-24.04-arm"
            arch: "powerpc64"
          - runs: "ubuntu-24.04-arm"
            arch: "sparc64"
          - runs: "ubuntu-24.04-arm"
            arch: "ppc64le"
          - runs: "ubuntu-24.04-arm"
            arch: "s390x"
          # Manual matrix excludes only; combos with no conf are auto-excluded.
          - release: "9.0"
            arch: aarch64
          - release: "9.0"
            arch: riscv64
          - release: "9.0"
            arch: powerpc64
          - release: "9.0"
            arch: sparc64
          - release: "9.0"
            arch: ppc64le
          - release: "9.0"
            arch: s390x
          - release: "9.1"
            arch: aarch64
          - release: "9.1"
            arch: riscv64
          - release: "9.1"
            arch: powerpc64
          - release: "9.1"
            arch: sparc64
          - release: "9.1"
            arch: ppc64le
          - release: "9.1"
            arch: s390x
          - release: "9.2"
            arch: aarch64
          - release: "9.2"
            arch: riscv64
          - release: "9.2"
            arch: powerpc64
          - release: "9.2"
            arch: sparc64
          - release: "9.2"
            arch: ppc64le
          - release: "9.2"
            arch: s390x
          - release: "9.3"
            arch: aarch64
          - release: "9.3"
            arch: riscv64
          - release: "9.3"
            arch: powerpc64
          - release: "9.3"
            arch: sparc64
          - release: "9.3"
            arch: ppc64le
          - release: "9.3"
            arch: s390x
          - release: "9.4"
            arch: aarch64
          - release: "9.4"
            arch: riscv64
          - release: "9.4"
            arch: powerpc64
          - release: "9.4"
            arch: sparc64
          - release: "9.4"
            arch: ppc64le
          - release: "9.4"
            arch: s390x
          - release: "10.0"
            arch: riscv64
          - release: "10.0"
            arch: powerpc64
          - release: "10.0"
            arch: ppc64le
          - release: "10.0"
            arch: s390x
          - release: "10.1"
            arch: riscv64
          - release: "10.1"
            arch: powerpc64
          - release: "10.1"
            arch: ppc64le
          - release: "10.1"
            arch: s390x
          - release: ""
            arch: riscv64
          - release: ""
            arch: powerpc64
          - release: ""
            arch: ppc64le
          - release: ""
            arch: s390x



    runs-on: ${{ matrix.runs }}
    name: rsync default NetBSD
    env:
      MYTOKEN : ${{ secrets.MYTOKEN }}
      MYTOKEN2: "value2"
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
    steps:
    - uses: actions/checkout@v6
    - name: Test in NetBSD
      if: ${{ contains(fromJSON('{"x86_64":["rsync","scp","sshfs","nfs"],"aarch64":["rsync","scp","sshfs","nfs"],"riscv64":[],"powerpc64":[],"sparc64":["rsync","scp","sshfs","nfs"],"ppc64le":[],"s390x":[]}')[matrix.arch == '' && 'x86_64' || matrix.arch], 'rsync') }}
      id: test
      uses: vmactions/netbsd-vm@d06a31b989792ff5af44a2913cd49457cbe86c71
      with:
        envs: 'MYTOKEN MYTOKEN2'
        prepare: |
          /usr/sbin/pkg_add -u curl
        mem: 4096
        release: ${{ matrix.release }}
        arch: ${{ matrix.arch }}
        nat: |
          "20022": "22"
          "8080": "80"
          "8443": "443"
          udp:"8081": "80"
        usesh: true
        debug: true
        debug-on-error: ${{ vars.DEBUG_ON_ERROR }}
        disable-cache: ${{ matrix.disableCache }}
        run: |
          env
          echo "TEST_ENV=abc" >>${GITHUB_ENV}
          cd $GITHUB_WORKSPACE;
          ls -lah
          whoami
          env
          /sbin/sysctl hw.model
          /sbin/sysctl hw.ncpu
          /sbin/sysctl hw.physmem
          /sbin/sysctl hw.usermem
          /usr/bin/vmstat

    - name: Test NAT
      if: ${{ contains(fromJSON('{"x86_64":["rsync","scp","sshfs","nfs"],"aarch64":["rsync","scp","sshfs","nfs"],"riscv64":[],"powerpc64":[],"sparc64":["rsync","scp","sshfs","nfs"],"ppc64le":[],"s390x":[]}')[matrix.arch == '' && 'x86_64' || matrix.arch], 'rsync') }}
      env:
        VM_USER: ""
      run: |
        set -e
        echo "TEST_ENV=$TEST_ENV"
        [ "$TEST_ENV" = "abc" ]
        ssh -v  -p 20022 ${VM_USER:-root}@localhost  "uname -a;whoami;pwd"

    - name: Test custom shell
      if: ${{ contains(fromJSON('{"x86_64":["rsync","scp","sshfs","nfs"],"aarch64":["rsync","scp","sshfs","nfs"],"riscv64":[],"powerpc64":[],"sparc64":["rsync","scp","sshfs","nfs"],"ppc64le":[],"s390x":[]}')[matrix.arch == '' && 'x86_64' || matrix.arch], 'rsync') }}
      shell: netbsd {0}
      run: |
        pwd
          cd $GITHUB_WORKSPACE;
          ls -lah
          whoami
          env
          /sbin/sysctl hw.model
          /sbin/sysctl hw.ncpu
          /sbin/sysctl hw.physmem
          /sbin/sysctl hw.usermem
          /usr/bin/vmstat








  testcustomshell:
    strategy:
      fail-fast: false
      matrix:
        release: [ "9.0", "9.1", "9.2", "9.3", "9.4", "10.0", "10.1"
, ""]
        arch: [ "aarch64", "", "riscv64", "powerpc64", "sparc64", "ppc64le", "s390x"]
        runs: [ "ubuntu-24.04", "ubuntu-24.04-arm"]
        exclude:
          - runs: "ubuntu-24.04-arm"
            arch: ""
          - runs: "ubuntu-24.04-arm"
            arch: "riscv64"
          - runs: "ubuntu-24.04-arm"
            arch: "powerpc64"
          - runs: "ubuntu-24.04-arm"
            arch: "sparc64"
          - runs: "ubuntu-24.04-arm"
            arch: "ppc64le"
          - runs: "ubuntu-24.04-arm"
            arch: "s390x"
          # Manual matrix excludes only; combos with no conf are auto-excluded.
          - release: "9.0"
            arch: aarch64
          - release: "9.0"
            arch: riscv64
          - release: "9.0"
            arch: powerpc64
          - release: "9.0"
            arch: sparc64
          - release: "9.0"
            arch: ppc64le
          - release: "9.0"
            arch: s390x
          - release: "9.1"
            arch: aarch64
          - release: "9.1"
            arch: riscv64
          - release: "9.1"
            arch: powerpc64
          - release: "9.1"
            arch: sparc64
          - release: "9.1"
            arch: ppc64le
          - release: "9.1"
            arch: s390x
          - release: "9.2"
            arch: aarch64
          - release: "9.2"
            arch: riscv64
          - release: "9.2"
            arch: powerpc64
          - release: "9.2"
            arch: sparc64
          - release: "9.2"
            arch: ppc64le
          - release: "9.2"
            arch: s390x
          - release: "9.3"
            arch: aarch64
          - release: "9.3"
            arch: riscv64
          - release: "9.3"
            arch: powerpc64
          - release: "9.3"
            arch: sparc64
          - release: "9.3"
            arch: ppc64le
          - release: "9.3"
            arch: s390x
          - release: "9.4"
            arch: aarch64
          - release: "9.4"
            arch: riscv64
          - release: "9.4"
            arch: powerpc64
          - release: "9.4"
            arch: sparc64
          - release: "9.4"
            arch: ppc64le
          - release: "9.4"
            arch: s390x
          - release: "10.0"
            arch: riscv64
          - release: "10.0"
            arch: powerpc64
          - release: "10.0"
            arch: ppc64le
          - release: "10.0"
            arch: s390x
          - release: "10.1"
            arch: riscv64
          - release: "10.1"
            arch: powerpc64
          - release: "10.1"
            arch: ppc64le
          - release: "10.1"
            arch: s390x
          - release: ""
            arch: riscv64
          - release: ""
            arch: powerpc64
          - release: ""
            arch: ppc64le
          - release: ""
            arch: s390x



    runs-on: ${{ matrix.runs }}
    name: customshell NetBSD
    env:
      MYTOKEN : ${{ secrets.MYTOKEN }}
      MYTOKEN2: "value2"
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
    steps:
    - uses: actions/checkout@v6
    - name: Test in NetBSD
      if: ${{ contains(fromJSON('{"x86_64":["rsync","scp","sshfs","nfs"],"aarch64":["rsync","scp","sshfs","nfs"],"riscv64":[],"powerpc64":[],"sparc64":["rsync","scp","sshfs","nfs"],"ppc64le":[],"s390x":[]}')[matrix.arch == '' && 'x86_64' || matrix.arch], 'rsync') }}
      id: test
      uses: vmactions/netbsd-vm@d06a31b989792ff5af44a2913cd49457cbe86c71
      with:
        envs: 'MYTOKEN MYTOKEN2'
        prepare: |
          /usr/sbin/pkg_add -u curl
        mem: 4096
        release: ${{ matrix.release }}
        arch: ${{ matrix.arch }}
        nat: |
          "20022": "22"
          "8080": "80"
          "8443": "443"
          udp:"8081": "80"
        usesh: true
        debug: true
        debug-on-error: ${{ vars.DEBUG_ON_ERROR }}
        custom-shell-name: vmsh
        run: |
          env
          echo "TEST_ENV=abc" >>${GITHUB_ENV}
          cd $GITHUB_WORKSPACE;
          ls -lah
          whoami
          env
          /sbin/sysctl hw.model
          /sbin/sysctl hw.ncpu
          /sbin/sysctl hw.physmem
          /sbin/sysctl hw.usermem
          /usr/bin/vmstat

    - name: Test NAT
      if: ${{ contains(fromJSON('{"x86_64":["rsync","scp","sshfs","nfs"],"aarch64":["rsync","scp","sshfs","nfs"],"riscv64":[],"powerpc64":[],"sparc64":["rsync","scp","sshfs","nfs"],"ppc64le":[],"s390x":[]}')[matrix.arch == '' && 'x86_64' || matrix.arch], 'rsync') }}
      env:
        VM_USER: ""
      run: |
        set -e
        echo "TEST_ENV=$TEST_ENV"
        [ "$TEST_ENV" = "abc" ]
        ssh -v  -p 20022 ${VM_USER:-root}@localhost  "uname -a;whoami;pwd"

    - name: Test custom shell
      if: ${{ contains(fromJSON('{"x86_64":["rsync","scp","sshfs","nfs"],"aarch64":["rsync","scp","sshfs","nfs"],"riscv64":[],"powerpc64":[],"sparc64":["rsync","scp","sshfs","nfs"],"ppc64le":[],"s390x":[]}')[matrix.arch == '' && 'x86_64' || matrix.arch], 'rsync') }}
      shell: vmsh {0}
      run: |
        pwd
          cd $GITHUB_WORKSPACE;
          ls -lah
          whoami
          env
          /sbin/sysctl hw.model
          /sbin/sysctl hw.ncpu
          /sbin/sysctl hw.physmem
          /sbin/sysctl hw.usermem
          /usr/bin/vmstat







  testerr:
    strategy:
      fail-fast: false
    runs-on: ubuntu-24.04
    name: test default error NetBSD
    steps:
    - uses: actions/checkout@v6
    - name: Test in NetBSD
      id: testerr
      continue-on-error: true
      uses: vmactions/netbsd-vm@d06a31b989792ff5af44a2913cd49457cbe86c71
      with:
        usesh: true
        debug: true
        run: |
          echo "this is an error"
          false
    - name: Verify that the previous step failed
      if: steps.testerr.outcome != 'failure'
      run: |
        echo "The step was expected to fail, but it succeeded!"
        exit 1
    - name: Verify that the previous step succeeded
      if: steps.testerr.outcome == 'failure'
      run: |
        echo "The step was expected to fail, and it failed!"
        exit 0

  testpass:
    strategy:
      fail-fast: false
    runs-on: ubuntu-24.04
    name: test default success NetBSD
    steps:
    - uses: actions/checkout@v6
    - name: Test in NetBSD
      id: testpass
      continue-on-error: true
      uses: vmactions/netbsd-vm@d06a31b989792ff5af44a2913cd49457cbe86c71
      with:
        usesh: true
        debug: true
        run: |
          echo "this is a normal command"
          true
    - name: Verify that the previous step succeeded
      if: steps.testpass.outcome != 'success'
      run: |
        echo "The step was expected to succeed, but it failed!"
        exit 1
    - name: Verify that the previous step failed
      if: steps.testpass.outcome == 'success'
      run: |
        echo "The step was expected to succeed, and it succeeded!"
        exit 0
